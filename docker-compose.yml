version: '3.8'

services:
  postgres:
    image: postgres:15
    command: postgres -c max_connections=300 -c shared_buffers=256MB
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./db/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
    profiles: ["rest", "grpc"]

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    profiles: ["rest", "grpc"]

  # REST Architecture Services
  gateway:
    build: ./rest/gateway
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - SEAT_SERVICE_URL=${SEAT_SERVICE_URL}
      - RESERVATION_SERVICE_URL=${RESERVATION_SERVICE_URL}
      - NOTIFY_SERVICE_URL=${NOTIFY_SERVICE_URL}
    depends_on:
      auth:
        condition: service_healthy
      seat:
        condition: service_healthy
      reservation:
        condition: service_healthy
      notify:
        condition: service_healthy
    profiles: ["rest"]

  auth:
    build: ./rest/auth
    ports:
      - "8081:8081"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
      - JWT_EXPIRATION_HOURS=${JWT_EXPIRATION_HOURS}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles: ["rest"]

  seat:
    build: ./rest/seat
    ports:
      - "8082:8082"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles: ["rest"]

  reservation:
    build: ./rest/reservation
    ports:
      - "8083:8083"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles: ["rest"]

  notify:
    build: ./rest/notify
    ports:
      - "8084:8084"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8084/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles: ["rest"]

  checkin_worker:
    build: ./rest/checkin_worker
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - GRACE_MINUTES=${GRACE_MINUTES}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    profiles: ["rest"]

  # gRPC Architecture Services (≥5 nodes requirement)
  # Load Balancer (nginx)
  grpc-lb:
    image: nginx:alpine
    ports:
      - "9090:9090"
    volumes:
      - ./grpc/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - grpc-app1
      - grpc-app2
      - grpc-app3
    profiles: ["grpc"]

  # gRPC App Instance 1
  grpc-app1:
    build: ./grpc/app
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
      - JWT_EXPIRATION_HOURS=${JWT_EXPIRATION_HOURS}
      - GRACE_MINUTES=${GRACE_MINUTES}
      - INSTANCE_ID=1
      - RAFT_NODE_ID=grpc-app1
      - RAFT_SELF_ADDRESS=grpc-app1:9090
      - RAFT_PEERS=grpc-app1@grpc-app1:9090,grpc-app2@grpc-app2:9090,grpc-app3@grpc-app3:9090
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    profiles: ["grpc"]

  # gRPC App Instance 2
  grpc-app2:
    build: ./grpc/app
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
      - JWT_EXPIRATION_HOURS=${JWT_EXPIRATION_HOURS}
      - GRACE_MINUTES=${GRACE_MINUTES}
      - INSTANCE_ID=2
      - RAFT_NODE_ID=grpc-app2
      - RAFT_SELF_ADDRESS=grpc-app2:9090
      - RAFT_PEERS=grpc-app1@grpc-app1:9090,grpc-app2@grpc-app2:9090,grpc-app3@grpc-app3:9090
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    profiles: ["grpc"]

  # gRPC App Instance 3
  grpc-app3:
    build: ./grpc/app
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ALGORITHM=${JWT_ALGORITHM}
      - JWT_EXPIRATION_HOURS=${JWT_EXPIRATION_HOURS}
      - GRACE_MINUTES=${GRACE_MINUTES}
      - INSTANCE_ID=3
      - RAFT_NODE_ID=grpc-app3
      - RAFT_SELF_ADDRESS=grpc-app3:9090
      - RAFT_PEERS=grpc-app1@grpc-app1:9090,grpc-app2@grpc-app2:9090,grpc-app3@grpc-app3:9090
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    profiles: ["grpc"]

volumes:
  postgres_data:
